using System;

public class NavMesh {
	public NavMesh (int[,] level) {
		int worldHeight = level.GetLength (0);
		int worldWidth = level.GetLength (1);
		

		for (int y = 0; y < worldHeight; y++) {
			for (int x = 0; x < worldWidth; x++) {

			}
		}
	}
}

